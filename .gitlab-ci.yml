stages: 
  - build
  - error_clean
  - reggie_short
  - reggie_tracking
  - reggie_long
  - reggie_special

cache:
  key: "$CI_BUILD_REF"
  paths: 
    - build_gnu/    
    - build_intel/    
    - regressioncheck/examples

# ----------------------------------------------------------------------------------------------------------------------------------------------------
# Stage "build": Build with intel/gnu on check-in
# ----------------------------------------------------------------------------------------------------------------------------------------------------
build_gnu:
  stage: build
  tags:
    - withmodules
  script:
    - module load env/gnu ; rm -rf build_gnu || true 
    - mkdir -p build_gnu ; cd build_gnu ; cmake .. -DBOLTZPLATZ_BUILD_HDF5=OFF ; make all

build_intel:
  stage: build
  tags:
    - withmodules
  script:
    - module load env/intel ; rm -rf build_intel || true 
    - mkdir -p build_intel ; cd build_intel ; cmake .. -DBOLTZPLATZ_BUILD_HDF5=OFF ; make all

# ----------------------------------------------------------------------------------------------------------------------------------------------------
# Stage "clean": delete build folders if previous builds fail and exit
# ----------------------------------------------------------------------------------------------------------------------------------------------------
clean:
  stage: error_clean
  tags:
    - withmodules
  script:
    - rm -rf build_gnu
    - rm -rf build_intel
  when: on_failure

# ----------------------------------------------------------------------------------------------------------------------------------------------------
# Stage "reggie_short": Run most simple reggie with previous builds on check-in
# ----------------------------------------------------------------------------------------------------------------------------------------------------
gnu:
  stage: reggie_short
  tags:
    - withmodules
  script:
    - module load env/gnu ; cd build_gnu/bin/ ; ./regressioncheck

intel:
  stage: reggie_short
  tags:
    - withmodules
  script:
    - module load env/intel ; cd build_intel/bin/ ; ./regressioncheck


# ----------------------------------------------------------------------------------------------------------------------------------------------------
# Stage "reggie_tracking": Build and run tracking examples on nightly
# ----------------------------------------------------------------------------------------------------------------------------------------------------
gnu_tracking:
  stage: reggie_tracking
  tags:
    - withmodules
  script:
    - if [ -n "${DO_NIGHTLY}" ]; then module load env/gnu ; cd build_gnu/bin/ ; ./regressioncheck build feature_tracking_DSMC 2; fi

intel_tracking:
  stage: reggie_tracking
  tags:
    - withmodules
  script:
    - echo "nothing to do";
    # fix h5diff on runner -> libs may be wrongly set in the binary file
    # "/opt/hdf5/hdf5-1.8.17/ifort16/bin/h5diff: symbol lookup error: /opt/hdf5/hdf5-1.8.17/ifort16/bin/h5diff: undefined symbol: ompi_mpi_char";
    #- if [ -n "${DO_NIGHTLY}" ]; then module load env/intel ; cd build_intel/bin/ ; ./regressioncheck build feature_tracking_DSMC 2; fi

# ----------------------------------------------------------------------------------------------------------------------------------------------------
# Stage "reggie_long": Build and run
#                         - basic PIC tests
#                         - build all specified compile options in "run_basic"
#                         - h-p-convtests
#                         - compile HDF5
#                      examples on nightly
# ----------------------------------------------------------------------------------------------------------------------------------------------------
gnu_mpi:
  stage: reggie_long
  tags:
    - withmodules
  script:
    - if [ -n "${DO_NIGHTLY}" ]; then module load env/gnu ; cd build_gnu/bin/ ; ./regressioncheck build 2; fi

gnu_PIC:
  stage: reggie_long
  tags:
    - withmodules
  script:
    - if [ -n "${DO_NIGHTLY}" ]; then module load env/gnu ; cd build_gnu/bin/ ; ./regressioncheck build feature_PIC 2; fi

gnu_convtest:
  stage: reggie_long
  tags:
    - withmodules
  script:
    - if [ -n "${DO_NIGHTLY}" ]; then module load env/gnu ; cd build_gnu/bin/ ; ./regressioncheck build feature_convtest 2; fi


gnu_HDF5_single_mpi:
  stage: reggie_long
  tags:
    - withmodules
  script:
    - if [ -n "${DO_NIGHTLY}" ]; then module load env/gnu ; cd build_gnu/bin/ ; ./regressioncheck build build_HDF5_single_MPI no-debug 2; fi



intel_mpi:
  stage: reggie_long
  tags:
    - withmodules
  script:
    - if [ -n "${DO_NIGHTLY}" ]; then module load env/intel ; cd build_intel/bin/ ; ./regressioncheck build 2; fi

intel_PIC:
  stage: reggie_long
  tags:
    - withmodules
  script:
    - echo "nothing to do";
    # fix h5diff on runner -> libs may be wrongly set in the binary file
    # "/opt/hdf5/hdf5-1.8.17/ifort16/bin/h5diff: symbol lookup error: /opt/hdf5/hdf5-1.8.17/ifort16/bin/h5diff: undefined symbol: ompi_mpi_char";
    #- if [ -n "${DO_NIGHTLY}" ]; then module load env/intel ; cd build_intel/bin/ ; ./regressioncheck build feature_PIC 2; fi

intel_convtest:
  stage: reggie_long
  tags:
    - withmodules
  script:
    - if [ -n "${DO_NIGHTLY}" ]; then module load env/intel ; cd build_intel/bin/ ; ./regressioncheck build feature_convtest 2; fi

intel_HDF5_single_mpi:
  stage: reggie_long
  tags:
    - withmodules
  script:
    - if [ -n "${DO_NIGHTLY}" ]; then module load env/intel ; cd build_intel/bin/ ; ./regressioncheck build build_HDF5_single_MPI no-debug 2; fi


# ----------------------------------------------------------------------------------------------------------------------------------------------------
# Stage "reggie_special": Run test for TWT with recordpoints
# ----------------------------------------------------------------------------------------------------------------------------------------------------
gnu_TWT:
  stage: reggie_special
  tags:
    - withmodules
  script:
    - module load env/gnu ; cd build_gnu/bin/ ; ./regressioncheck run feature_TWT_recordpoints

intel_TWT:
  stage: reggie_special
  tags:
    - withmodules
  script:
    - echo "nothing to do";
    # fix h5diff on runner -> libs may be wrongly set in the binary file
    #- module load env/intel ; cd build_intel/bin/ ; ./regressioncheck run feature_TWT_recordpoints
